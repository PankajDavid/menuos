{
  "name": "menuos-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd menuos/backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "cd menuos/backend && npm start",
    "dev": "cd menuos && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
